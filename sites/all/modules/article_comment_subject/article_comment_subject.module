<?php

function article_comment_subject_form_alter(&$form, &$form_state, $form_id) {
    if ($form_id == 'comment_node_article_form') {
        $form['subject'] = array('#type' => 'hidden');
        // print '<pre>';
        // print_r($form);
        // print '</pre>';
    }
}
